# 角色管理模块 - 判断-条件覆盖测试

**模块**：RoleServiceImpl | **日期**：2026-01-08 | **测试方法**：判断-条件覆盖

---

## 一、测试目标

同时满足判定覆盖和条件覆盖的要求。

---

## 二、复合判定分析

### updateRole() 中的关键判定

```
if ("SUPER_ADMIN".equalsIgnoreCase(code) || "admin".equalsIgnoreCase(code)) {
    if (dto.getEnable() != null && !dto.getEnable()) { // 禁用
        throw ...
    }
    if (dto.getPermissionIds() != null && dto.getPermissionIds().isEmpty()) { // 清空权限
        throw ...
    }
}
```

---

## 三、测试用例

| 用例ID | 判定结果 | 条件组合 | 输入 | 预期 |
|--------|----------|----------|------|------|
| JC01 | T | C1=T, C2=F | code=SUPER_ADMIN | 进入分支 |
| JC02 | T | C1=F, C2=T | code=admin | 进入分支 |
| JC03 | F | C1=F, C2=F | code=普通角色 | 跳过分支 |
| JC04 | 内层T | enable!=null && !enable | 禁用系统角色 | 异常 |
| JC05 | 内层F | enable=null | 不禁用 | 继续 |
| JC06 | 内层T | ids!=null && isEmpty | 清空权限 | 异常 |
| JC07 | 内层F | ids=null | 不清空 | 继续 |
| JC08 | createRole | code重复 | 已存在code | 异常 |
| JC09 | createRole | name重复 | 已存在name | 异常 |
| JC10 | deleteRole | 有用户 | 有关联用户 | 异常 |
| JC11 | deleteRole | 无用户 | 无关联用户 | 成功 |
| JC12 | deleteRole | 系统角色 | SUPER_ADMIN | 异常 |

---

**统计**：12个测试用例
