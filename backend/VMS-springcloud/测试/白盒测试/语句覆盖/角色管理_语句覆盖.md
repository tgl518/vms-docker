# 角色管理模块 - 语句覆盖测试

**模块**：RoleServiceImpl | **日期**：2026-01-08 | **测试方法**：语句覆盖

---

## 一、测试目标

确保 `createRole()`, `updateRole()`, `deleteRole()` 中的每条语句至少执行一次。

---

## 二、代码分析

### createRole() 语句清单
| 行号 | 语句 |
|------|------|
| 102 | `wrapper.eq(Role::getCode, dto.getCode())` |
| 103 | `if (roleMapper.selectCount(wrapper) > 0)` |
| 104 | `throw new BusinessException(...)` |
| 108 | `wrapper.eq(Role::getName, dto.getName())` |
| 109 | `if (roleMapper.selectCount(wrapper) > 0)` |
| 110 | `throw new BusinessException(...)` |
| 113-118 | 创建Role对象并赋值 |
| 120 | `roleMapper.insert(role)` |
| 123-125 | 保存权限关联 |
| 127 | `return role.getId()` |

---

## 三、测试用例

| 用例ID | 用例名称 | 输入 | 覆盖语句 | 预期结果 |
|--------|----------|------|----------|----------|
| SC01 | 创建成功-完整流程 | 有效code/name + permissionIds | 102-127 | 成功 |
| SC02 | 创建失败-code重复 | code="SUPER_ADMIN" | 102-104 | 异常 |
| SC03 | 创建失败-name重复 | name="超级管理员" | 108-110 | 异常 |
| SC04 | 创建成功-无权限 | 有效code/name, permissionIds=null | 102-120,127 | 成功 |
| SC05 | 更新成功-完整流程 | 存在的id + 修改信息 | updateRole全部 | 成功 |
| SC06 | 更新失败-不存在 | id=99999 | 135-137 | 异常 |
| SC07 | 删除成功 | 普通角色id | deleteRole全部 | 成功 |
| SC08 | 删除失败-系统角色 | id=1 | 191-198 | 异常 |

---

**统计**：8个测试用例，覆盖所有主要语句
