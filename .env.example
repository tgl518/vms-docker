# VMS Docker 环境变量配置模板
# 
# 说明：此文件为可选配置。项目已配置合理的默认值，可直接运行 docker-compose up -d
# 如需自定义配置，请复制此文件为 .env 并修改相应值

# ==================== 生产环境建议修改 ====================
# 以下配置在生产环境中建议修改，开发测试可使用默认值

# MySQL root 密码 (默认: password)
# MYSQL_ROOT_PASSWORD=your_secure_password

# JWT 密钥 (默认: VmsSpringCloudSecretKeyForJwtTokenGeneration2024)
# JWT_SECRET=your_jwt_secret_key

# ==================== 一般无需修改 ====================
# 以下配置通常无需修改

# MySQL 数据库名 (默认: vms_cloud)
# MYSQL_DATABASE=vms_cloud

# 文件上传路径 (默认: /app/media)
# UPLOAD_PATH=/app/media
