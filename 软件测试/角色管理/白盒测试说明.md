# 角色管理模块 - 白盒测试说明

> 测试文件位置：`backend/VMS-springcloud/vms-user/src/test/java/cn/yznu/vms/user/service/impl/RoleServiceImplTest.java`

---

## 测试覆盖目标

| 覆盖类型 | 说明 | 达成情况 |
|---------|------|---------|
| **语句覆盖** | 所有语句至少执行一次 | ✅ 已覆盖 |
| **判定覆盖** | 所有分支 (if-else) 都被测试 | ✅ 已覆盖 |
| **条件覆盖** | 所有条件的真假值都被测试 | ✅ 已覆盖 |
| **路径覆盖** | 所有可能的执行路径都被测试 | ✅ 已覆盖 |

---

## 测试类结构

```java
@DisplayName("RoleServiceImpl 角色管理服务白盒测试")
class RoleServiceImplTest {
    
    @Nested @DisplayName("创建角色 (createRole) 测试")
    class CreateRoleTests { ... }
    
    @Nested @DisplayName("更新角色 (updateRole) 测试")  
    class UpdateRoleTests { ... }
    
    @Nested @DisplayName("删除角色 (deleteRole) 测试")
    class DeleteRoleTests { ... }
    
    @Nested @DisplayName("查询角色测试")
    class QueryRoleTests { ... }
    
    @Nested @DisplayName("缓存同步 (syncRolePermissionCache) 测试")
    class CacheSyncTests { ... }
    
    @Nested @DisplayName("用户-角色关联操作测试")
    class UserRoleTests { ... }
}
```

---

## 测试用例汇总

### 创建角色 (createRole) - 6 个测试

| 用例 | 测试路径 | 覆盖类型 |
|------|---------|---------|
| 创建成功_当编码和名称都不重复 | 正常路径 | 语句+路径 |
| 创建失败_当角色编码已存在 | 异常路径1 | 判定 |
| 创建失败_当角色名称已存在 | 异常路径2 | 条件 |
| 创建成功_当权限列表为空时跳过权限保存 | 分支路径 | 条件 |
| 创建成功_当排序为null时使用默认值0 | 边界路径 | 条件 |
| 创建成功_当enable为false时状态为DISABLED | 状态分支 | 条件 |

### 更新角色 (updateRole) - 7 个测试

| 用例 | 测试路径 | 覆盖类型 |
|------|---------|---------|
| 更新成功_当角色存在且参数有效 | 正常路径 | 语句+路径 |
| 更新失败_当角色不存在 | 异常路径 | 判定 |
| 更新失败_当尝试禁用SUPER_ADMIN角色 | 保护逻辑 | 判定 |
| 更新失败_当尝试禁用admin角色 | 保护逻辑 | 条件 |
| 更新失败_当尝试清空系统角色权限 | 保护逻辑 | 条件 |
| 更新失败_当新名称与其他角色重复 | 冲突检测 | 判定 |
| 更新成功_当修改权限时同步Redis缓存 | 缓存路径 | 路径 |

### 删除角色 (deleteRole) - 5 个测试

| 用例 | 测试路径 | 覆盖类型 |
|------|---------|---------|
| 删除成功_当角色存在且无用户关联 | 正常路径 | 语句+路径 |
| 删除失败_当角色不存在 | 异常路径 | 判定 |
| 删除失败_当尝试删除SUPER_ADMIN | 保护逻辑 | 判定 |
| 删除失败_当尝试删除admin | 保护逻辑 | 条件 |
| 删除失败_当角色下有关联用户 | 关联检测 | 判定 |

### 查询角色 - 4 个测试

| 用例 | 测试路径 | 覆盖类型 |
|------|---------|---------|
| 查询成功_当角色存在 | 正常路径 | 语句 |
| 查询失败_当角色不存在 | 异常路径 | 判定 |
| 分页查询_带名称和启用状态筛选 | 筛选路径 | 条件 |
| 分页查询_返回空列表 | 边界路径 | 条件 |

### 缓存同步 - 4 个测试

| 用例 | 测试路径 | 覆盖类型 |
|------|---------|---------|
| 同步成功_当角色存在且启用且有权限 | 正常路径 | 语句+路径 |
| 同步时_角色不存在只清除缓存 | 分支路径 | 条件 |
| 同步时_角色被禁用只清除缓存 | 分支路径 | 条件 |
| 同步时_角色无权限不添加缓存 | 边界路径 | 条件 |

### 用户-角色关联 - 5 个测试

| 用例 | 测试路径 | 覆盖类型 |
|------|---------|---------|
| 获取角色用户列表 | 正常路径 | 语句 |
| 给角色添加用户_正常情况 | 正常路径 | 语句 |
| 给角色添加用户_空列表时跳过 | 边界路径 | 条件 |
| 从角色移除用户_正常情况 | 正常路径 | 语句 |
| 从角色移除用户_空列表时跳过 | 边界路径 | 条件 |

---

## 运行测试命令

```bash
# 进入项目目录
cd backend/VMS-springcloud

# 运行所有角色管理测试
mvn test -Dtest=RoleServiceImplTest

# 运行单个测试类
mvn test -Dtest=RoleServiceImplTest#CreateRoleTests

# 生成测试报告
mvn test -Dtest=RoleServiceImplTest jacoco:report
```

---

## 代码覆盖率目标

| 指标 | 目标 | 预期 |
|------|------|------|
| 行覆盖率 | ≥80% | ~95% |
| 分支覆盖率 | ≥70% | ~90% |
| 方法覆盖率 | 100% | 100% |
