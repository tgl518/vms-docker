# 是否使用Hash路由
VITE_USE_HASH = 'true'

# 资源公共路径,需要以 /开头和结尾
VITE_PUBLIC_PATH = '/'

# Axios 基础路径 (使用/api前缀，配合vite代理)
VITE_AXIOS_BASE_URL = '/api'

# 代理配置-target (指向Spring Cloud Gateway)
VITE_PROXY_TARGET = 'http://localhost:8080'
